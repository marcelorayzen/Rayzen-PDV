export const featureLayer = {
  name: "features",
  responsibility: "Compose operator-facing flows without embedding persistence details."
} as const;

export { pdvShellFeature } from "./pdv-shell.js";
