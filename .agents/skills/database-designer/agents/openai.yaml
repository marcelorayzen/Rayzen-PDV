interface:
  display_name: "PDV Rayzen - Database Designer"
  short_description: "Schema, migrations e integridade do SQLite"
  default_prompt: "Use $database-designer para revisar schema, migrations e rollback do PDV Rayzen."
policy:
  allow_implicit_invocation: false
